// Learner dashboard (individual or org-affiliated).
// Will surface: enrolled courses, progress (TRK-*), quiz results (REV-*),
// downloadable certificates (CERT-03).

export default function LearnerDashboardPage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>My Courses</h1>
      <p>Placeholder. Enrolled courses, progress, and certificate downloads go here.</p>
    </main>
  );
}
