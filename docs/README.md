# docs/

Place the canonical Business Requirements Document here (e.g. as
`BRD.md` or `BRD.docx`) once finalized, or link to wherever it's stored if
it lives outside the repo.

Technical design documents (data model rationale, ADRs, API contracts)
should also live here as they're produced, e.g.:

- `BRD.md`
- `data-model.md`
- `api-contracts.md`
- `adr/0001-stack-choice-nextjs-vs-spring.md` — worth writing this one down,
  since the stack decision was revisited mid-project (Spring → Next.js).
